"""
Authentication models - MVP scope (G01_F01).

Token Strategy: Stateless JWT (no database storage).
- Email verification tokens: Sent in email link, verified by signature
- Future tokens (SF02): 7-day access tokens, stateless validation
"""

# No models needed - using stateless JWT tokens only

